# Microsoft Defender adapter

Reserved integration boundary for future Defender context. No production connector ships in the MVP. Endpoint response remains approval-gated through Tawny; this directory prevents Defender-specific semantics from leaking into the core domain.
