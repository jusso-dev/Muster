# Microsoft Entra adapter

Entra supports Better Auth OIDC sign-in and a future approval-gated identity response adapter. Identity disable is not enabled in the MVP. Any future state-changing operation must bind an approval, require a dedicated capability, use an idempotency key, and write room, investigation, and audit events.
