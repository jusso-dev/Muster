# Kelpie adapter

Kelpie is authoritative for formal case lifecycle. The local mock exposes deterministic case-create and history responses for the demonstration workflow and always returns a visible mock marker. See `docs/integrations/current-upstream-contracts.md`.
