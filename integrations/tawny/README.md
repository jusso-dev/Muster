# Tawny adapter

Tawny is authoritative for endpoint telemetry and bounded response. The local mock exposes deterministic hunt, endpoint, and isolation-result responses and always returns a visible mock marker. Isolation still requires a Muster approval.
