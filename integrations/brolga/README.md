# Brolga adapter

Brolga is authoritative for normalised threat-intelligence context packs.
The local mock returns a deterministic pack with a visible mock marker.
`disposition: unknown` means not seen — never treat it as benign.
