# Microsoft Sentinel adapter

Initial adapter scope is read-only: Entra client credentials, workspace selection, incident polling, Log Analytics queries, analytics-rule read, entity lookup, templates, rate limiting, and query audit. Query result bodies remain in the authorised integration store; room messages reference bounded results. Destructive Sentinel operations are unavailable in the MVP.
