# Terraform deployment

Reserved infrastructure-module boundary. Cloud-specific modules must provision private PostgreSQL, Redis, object storage, secret references, encryption, backup, observability, and restricted egress without embedding credentials in state.
